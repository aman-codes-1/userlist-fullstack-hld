apiVersion: v1
kind: Service
metadata:
  name: api-gateway-lb
spec:
  type: LoadBalancer
  selector:
    io.kompose.service: api-gateway
  ports:
    - name: http-main
      port: 80
      targetPort: 4000
    - name: http-alt
      port: 81
      targetPort: 4002
