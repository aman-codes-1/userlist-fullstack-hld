apiVersion: v1
kind: Service
metadata:
  name: mongodb
  labels:
    app: mongodb
    io.kompose.service: mongodb
spec:
  selector:
    app: mongodb
  ports:
    - name: "27017"
      port: 27017
      targetPort: 27017
