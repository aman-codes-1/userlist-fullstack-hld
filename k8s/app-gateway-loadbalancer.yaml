apiVersion: v1
kind: Service
metadata:
  name: app-gateway-lb
  labels:
    app: app-gateway
spec:
  type: LoadBalancer
  selector:
    app: app-gateway
  ports:
    - port: 80
      targetPort: 4001
