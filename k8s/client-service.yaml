apiVersion: v1
kind: Service
metadata:
  name: client
  labels:
    app: client
    io.kompose.service: client
spec:
  selector:
    app: client
  ports:
    - name: http
      port: 3001
      targetPort: 3001
