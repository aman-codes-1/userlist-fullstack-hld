apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  labels:
    app: api-gateway
    io.kompose.service: api-gateway
spec:
  selector:
    app: api-gateway
  ports:
    - name: "4000"
      port: 4000
      targetPort: 4000
    - name: "4002"
      port: 4002
      targetPort: 4002
