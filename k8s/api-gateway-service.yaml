apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.35.0 (HEAD)
  labels:
    io.kompose.service: api-gateway
  name: api-gateway
spec:
  ports:
    - name: "4000"
      port: 4000
      targetPort: 4000
    - name: "4002"
      port: 4002
      targetPort: 4002
  selector:
    io.kompose.service: api-gateway
