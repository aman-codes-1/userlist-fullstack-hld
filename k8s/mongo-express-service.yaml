apiVersion: v1
kind: Service
metadata:
  name: mongo-express
  labels:
    app: mongo-express
    io.kompose.service: mongo-express
spec:
  selector:
    app: mongo-express
  ports:
    - name: http
      port: 8081
      targetPort: 8081
